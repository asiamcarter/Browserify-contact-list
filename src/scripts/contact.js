const contact = {
    contactHTML(value) {
       return `
        <h3>Name: ${value}</h3>
        <p>Phone Number: ${value}</p>
        <p>Address: ${value}</p>
        `
    }
}

export default contact